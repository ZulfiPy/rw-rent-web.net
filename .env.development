# The API the development app talks to. Cross-origin with credentials: the backend's CORS
# trusts exactly http://localhost:5173, which is the port `npm run dev` must use.
VITE_API_BASE_URL=http://localhost:5001
